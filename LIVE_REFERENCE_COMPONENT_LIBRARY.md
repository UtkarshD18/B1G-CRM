# Live Reference Component Library

This library indexes every interactive widget, card, table, filter, and action discovered during the crawl of the reference CRM at `https://crm.oneoftheprojects.com`.

Use this document as the source of truth for UI parity during Sprint 3 implementation.

## Page: `/admin?page=dashboard` (ADMIN)

### 1. Widgets & Cards
- **0** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**

---

## Page: `/admin?page=manage-plans` (ADMIN)

### 1. Widgets & Cards
- **0** (Visual container panel)
- **499** (Visual container panel)
- **1899** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add New Plan"**
- Button: **"Edit"**
- Button: **"Delete Plan"**

---

## Page: `/admin?page=manage-users` (ADMIN)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `AUTO LOGIN`, `NAME`, `MOBILE NUMBER`, `TIMEZONE`, `PLAN`, `PLAN EXPIRES`, `ACTIONS`
  - Row count: 2

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Refresh"**
- Button: **"Columns"**
- Button: **"0
Filters"**
- Button: **"Density"**
- Button: **"Export"**

---

## Page: `/admin?page=orders` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `PAYMENT METHOD`, `AMOUNT`, `STATUS`, `NAME`, `EMAIL`, `DATE`, `ACTIONS`
  - Row count: 9

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search..."** | Element: `input` (type=`search` placeholder=`Search...` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Refresh"**
- Button: **"0
Filters"**
- Button: **"Export"**

---

## Page: `/admin?page=front-partner` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`file` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Refresh"**

---

## Page: `/admin?page=faq` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Question"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Answer"** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Refresh"**
- Button: **"Add FAQ"**

---

## Page: `/admin?page=manage-page` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Page Title"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"<p>Hello Worlds</p>"** | Element: `textarea` (type=`textarea` placeholder=`<p>Hello Worlds</p>` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`file` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Refresh"**
- Button: **"Privacy Policy"**
- Button: **"Terms & Conditions"**
- Button: **"Add"**

---

## Page: `/admin?page=testimonial` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Title"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Description"** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **"Reviewer Name"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Reviewer Position"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Refresh"**
- Button: **"Add"**

---

## Page: `/admin?page=contact-form` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `Email`, `Name`, `Mobile`, `Message`, `Actions`
  - Row count: 5

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"0
Filters"**
- Button: **"Export"**

---

## Page: `/admin?page=payment-gateways` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Key ID"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Secret Key"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Key ID"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Secret Key"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Key ID"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Secret Key"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Key ID"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Secret Key"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Public Key"** | Element: `input` (type=`text` placeholder=`APP_USR-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx` name=`none`)
- Label: **"Access Token"** | Element: `input` (type=`text` placeholder=`APP_USR-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Title"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Description"** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save Settings"**

---

## Page: `/admin?page=smtp` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Email Address"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Username"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"SMTP Host"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"SMTP Port"** | Element: `input` (type=`number` placeholder=`none` name=`none`)
- Label: **"Password"** | Element: `input` (type=`password` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save"**
- Button: **"Test Email"**

---

## Page: `/admin?page=web-theme` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create New Theme"**
- Button: **"Edit"**
- Button: **"Set Active"**

---

## Page: `/admin?page=social-login` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Key ID"** | Element: `input` (type=`text` placeholder=`Enter your Google Client ID` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"App ID"** | Element: `input` (type=`text` placeholder=`Enter your Facebook App ID` name=`none`)
- Label: **"App Secret"** | Element: `input` (type=`password` placeholder=`Enter your Facebook App Secret` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save"**

---

## Page: `/admin?page=site-settings` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`file` placeholder=`none` name=`none`)
- Label: **"WhatsCRM"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"https://example.com"** | Element: `input` (type=`text` placeholder=`https://example.com` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **"Currency Code"** | Element: `input` (type=`text` placeholder=`USD` name=`none`)
- Label: **"Currency Symbol"** | Element: `input` (type=`text` placeholder=`$` name=`none`)
- Label: **"Exchange Rate"** | Element: `input` (type=`number` placeholder=`1.0` name=`none`)
- Label: **"Meta Description"** | Element: `textarea` (type=`textarea` placeholder=`Enter a description for search engines...` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **"Home Page Tutorial"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Chatbot Tutorial"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Broadcast Tutorial"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save Changes"**

---

## Page: `/admin?page=translation` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add New Language"**
- Button: **"Edit"**
- Button: **"Delete"**
- Button: **"Set Default"**

---

## Page: `/admin?page=update-web` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Update Now"**

---

## Page: `/admin?page=wa-link` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `Email`, `Mobile`, `Message`, `Created At`, `Actions`
  - Row count: 3

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"0
Filters"**
- Button: **"Export"**

---

## Page: `/admin?page=flow-builder-template` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add New"**

---

## Page: `/admin?page=qr-plugin-settings` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save"**

---

## Page: `/admin?page=instagram-config` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"App ID"** | Element: `input` (type=`text` placeholder=`e.g. 2016106878976830` name=`none`)
- Label: **"App Secret"** | Element: `input` (type=`text` placeholder=`e.g. 132299b722a3dff9...` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save Settings"**
- Button: **"Copy to clipboard"**

---

## Page: `/admin?page=web-notification` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"API Key"** | Element: `input` (type=`password` placeholder=`Enter value` name=`none`)
- Label: **"Auth Domain"** | Element: `input` (type=`text` placeholder=`Enter value` name=`none`)
- Label: **"Project ID"** | Element: `input` (type=`text` placeholder=`Enter value` name=`none`)
- Label: **"Storage Bucket"** | Element: `input` (type=`text` placeholder=`Enter value` name=`none`)
- Label: **"Messaging Sender ID"** | Element: `input` (type=`text` placeholder=`Enter value` name=`none`)
- Label: **"App ID"** | Element: `input` (type=`password` placeholder=`Enter value` name=`none`)
- Label: **"Measurement ID"** | Element: `input` (type=`text` placeholder=`Enter value` name=`none`)
- Label: **"VAPID Key"** | Element: `input` (type=`password` placeholder=`Enter value` name=`none`)
- Label: **"Client Email"** | Element: `input` (type=`text` placeholder=`Enter value` name=`none`)
- Label: **"Private Key"** | Element: `input` (type=`password` placeholder=`Enter value` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save Changes"**

---

## Page: `/admin?page=send-web-push` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Notification Title"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Notification Body"** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **"Click URL (optional)"** | Element: `input` (type=`text` placeholder=`https://...` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Text Only"**
- Button: **"With Image"**
- Button: **"Send Now (2 recipients)"**

---

## Page: `/admin?page=embed-config` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"App Secret"** | Element: `input` (type=`password` placeholder=`none` name=`none`)
- Label: **"App ID"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"WA Embed Login"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save"**
- Button: **"Copy to clipboard"**

---

## Page: `/admin?page=telegram-config` (ADMIN)

### 1. Widgets & Cards
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Telegram API ID"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Telegram API Hash"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Save"**
- Button: **"my.telegram.org"**

---

## Page: `/user?page=dashboard` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=inbox` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search…"** | Element: `input` (type=`text` placeholder=`Search…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**

---

## Page: `/user?page=kanban` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=phonebook` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: ``, `NAME`, `PHONEBOOK`, `MOBILE NUMBER`, `VARIABLE 1`, `VARIABLE 2`, `VARIABLE 3`, `VARIABLE 4`, `VARIABLE 5`, `DATE`
  - Row count: 3

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"New phonebook name"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add Phonebook"**
- Button: **"Export"**

---

## Page: `/user?page=campaign-dashboard` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Your dashboard overview"**
- Button: **"All Campaigns"**
- Button: **"View All"**
- Button: **"Details"**

---

## Page: `/user?page=automation-flows` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Source"** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Flow title…"** | Element: `input` (type=`text` placeholder=`Flow title…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**

---

## Page: `/user?page=wa-chatbot` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add Chatbot"**

---

## Page: `/user?page=integrations` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=add-whatsapp-qr` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=agent-login` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `AUTO LOGIN`, `NAME`, `EMAIL`, `MOBILE NUMBER`, `COMMENTS`, `ACTIVE`, `CHATS`, `ACTIONS`, `DELETE`
  - Row count: 3

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`password` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`number` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add Agent"**

---

## Page: `/user?page=agent-task` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `EMAIL`, `TITLE`, `DESCRIPTION`, `STATUS`, `AGENT COMMENT`, `ADDED ON`, `VIEW PAGE`, `DELETE`
  - Row count: 2

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `textarea` (type=`textarea` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add Task"**

---

## Page: `/user?page=chat-widget` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: `LOGO`, `TITLE`, `WHATSAPP NUMBER`, `SIZE`, `PLACEMENT`, `COPY CODE`, `TEST`, `DELETE`
  - Row count: 2

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`number` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`range` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add Widget"**

---

## Page: `/user?page=billing` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=api-dashboard` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: ``, `KEY ID`, `MESSAGE ID`, `STATUS`, `REQUEST`, `RESPONSE`, `CREATED AT`
  - Row count: 3

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"0
Filters"**
- Button: **"Export"**

---

## Page: `/user?page=manage-webhooks` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=create-meta-template` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"e.g. order_confirmation"** | Element: `input` (type=`text` placeholder=`e.g. order_confirmation` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Back"**
- Button: **"Hide Preview"**
- Button: **"Next"**

---

## Page: `/user?page=settings` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=whatsapp-forms` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=link-instagram` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add"**

---

## Page: `/user?page=whatsapp-warmer` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=create-call-flow` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Welcome message"** | Element: `textarea` (type=`textarea` placeholder=`Welcome message` name=`none`)
- Label: **"sk-..."** | Element: `input` (type=`password` placeholder=`sk-...` name=`none`)
- Label: **"System instructions"** | Element: `textarea` (type=`textarea` placeholder=`System instructions` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**

---

## Page: `/user?page=wa-call-logs` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: ``, `Status`, `Caller`, `Started At`, `Duration`, `Transcription`, `Recordings`
  - Row count: 1

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**

---

## Page: `/user?page=setup-wa-calls` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/user?page=webhook-automation` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add Webhook"**

---

## Page: `/user?page=webhook-logs` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- **Data Table #1**
  - Headers: ``, `KEY ID`, `NAME`, `HTTP METHOD`, `EVENT TYPE`, `STATUS`, `CREATED AT`, `ACTIONS`
  - Row count: 11

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`checkbox` placeholder=`none` name=`none`)
- Label: **""** | Element: `input` (type=`text` placeholder=`none` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Columns"**
- Button: **"0
Filters"**
- Button: **"Density"**
- Button: **"Export"**

---

## Page: `/user?page=telegram-sessions` (USER)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Add New Session"**

---

## Page: `/user?page=web-notification` (USER)

### 1. Widgets & Cards
- **2** (Visual container panel)
- **0** (Visual container panel)
- **1** (Visual container panel)
- **Panel** (Visual container panel)

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)
- Label: **"Search menu…"** | Element: `input` (type=`text` placeholder=`Search menu…` name=`none`)

### 4. Interactive Actions & Buttons
- Button: **"English"**
- Button: **"Create Chatbot"**

---

## Page: `/agent?page=dashboard` (AGENT)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- Label: **"Search…"** | Element: `input` (type=`text` placeholder=`Search…` name=`none`)

### 4. Interactive Actions & Buttons
- *None detected.*

---

## Page: `/agent?page=inbox` (AGENT)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- *None detected.*

### 4. Interactive Actions & Buttons
- *None detected.*

---

## Page: `/agent?page=chats` (AGENT)

### 1. Widgets & Cards
- *None detected.*

### 2. Tables & Data Grids
- *None detected.*

### 3. Filters & Inputs
- *None detected.*

### 4. Interactive Actions & Buttons
- *None detected.*

---

