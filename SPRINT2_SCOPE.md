# Sprint 2 Approved Scope

Refinement items approved for implementation in Sprint 2, focusing on quality refinement, visual polish, and conversation capabilities.

## Approved Scope List

1. **Inbox Inline Media Previews**: Refactor message bubble rendering to support visual image grids, video players, audio player tags, and download links for documents inside conversation threads instead of text URLs.
2. **Card Subtext Readability overrides**: Restructure selector declarations inside `App.css` to override unreadable white text styles on beige cards.
3. **Table actions margins**: Add gap spacing for action controls inside cell grids to fix collisions when wrapping.

## Out of Scope (Staged for Future Sprints)
- Baileys QR sync rewrite (requires full integration pass).
- CMS dashboard charts replacement.
- Webhook logging migration.
